---
phase: 18.3-rephrase-card-panel-sizing-fix-inserted
plan: 02
subsystem: ui
tags: [appkit, nspanel, nsscrollview, nshostingview, panel-positioner, sizing]

requires:
  - phase: 18.3-01
    provides: Inner SwiftUI ScrollView removed from RephraseCardView — fittingSize now reports true content height

provides:
  - PanelPositioner.capHeight static helper (D-08 option a)
  - RephraseCardPanelController height cap at visibleFrame.height - 40 (D-04/D-06/D-09)
  - Conditional NSScrollView wrapper when fittingSize overflows cap (D-05)
  - internal testHookPanel accessor for Plan 03 D-11 test inspection

affects:
  - 18.3-03 (test plan — uses testHookPanel, exercises capHeight + scroll wrapper)

tech-stack:
  added: []
  patterns:
    - "PanelPositioner capHeight: pre-pass helper (option a) returns capped NSSize; callers opt in explicitly before marginOrigin"
    - "Conditional NSScrollView wrapper: drawsBackground=false, borderType=.noBorder, autohidesScrollers=true, documentView=hosting"
    - "Controller scrollWrapper teardown: nil alongside panel + hostingView in hide() for symmetry"
    - "testHookPanel: internal read-only computed property exposes private panel to @testable consumers without widening storage"

key-files:
  created: []
  modified:
    - OpenGram/SuggestionUI/Panels/PanelPositioner.swift
    - OpenGram/SuggestionUI/Panels/RephraseCardPanelController.swift

key-decisions:
  - "capHeight placed before clampedY in PanelPositioner enum — option a (explicit opt-in) per D-08 rationale"
  - "verticalSafeMargin = 40pt as private static let on controller (D-06 — not exposed as config)"
  - "flooredSize (200pt floor) computed before capped — D-10 floor preserved in both branches"
  - "testHookPanel lives in this plan (not Plan 03) because RephraseCardPanelController.swift already in files_modified — keeps Plan 03 test-only per D-15"

patterns-established:
  - "Pre-pass height cap: call PanelPositioner.capHeight before marginOrigin to guarantee Y+height stays inside visibleFrame"
  - "Conditional scroll wrapper: capped.height < flooredSize.height triggers NSScrollView path; equality/smaller stays plain NSHostingView"

requirements-completed:
  - UAT-18.1-RESIDUAL-FOLLOWUP
  - UI-SPEC-D02
  - UI-SPEC-D04
  - UI-SPEC-D05
  - UI-SPEC-D06

duration: 8min
completed: 2026-04-17
---

# Phase 18.3 Plan 02: Rephrase Card Panel Sizing Fix Summary

**PanelPositioner.capHeight + conditional NSScrollView wrapper in RephraseCardPanelController cap panel height at visibleFrame.height - 40 and scroll overflow content so Accept/Dismiss remain reachable**

## Performance

- **Duration:** 8 min
- **Started:** 2026-04-17T16:15:00Z
- **Completed:** 2026-04-17T16:23:00Z
- **Tasks:** 2
- **Files modified:** 2

## Accomplishments

- `PanelPositioner.capHeight(_:visibleFrame:margin:)` added as public static helper; existing origin/marginOrigin/clampedY byte-unchanged
- `RephraseCardPanelController` floors fittingSize at 200pt (D-10), caps at `visibleFrame.height - 40` (D-09), installs `NSScrollView` wrapper only on overflow (D-05)
- `hide()` tears down `scrollWrapper = nil` symmetrically alongside panel and hostingView
- `internal var testHookPanel: NSPanel? { panel }` read-only accessor added for Plan 03 D-11 test cases

## Task Commits

1. **Task 1: Add PanelPositioner.capHeight helper** - `0ba5712` (feat)
2. **Task 2: Height cap + conditional NSScrollView + testHookPanel in RephraseCardPanelController** - `9cbf97a` (feat)

## Files Created/Modified

- `OpenGram/SuggestionUI/Panels/PanelPositioner.swift` - Added `capHeight` static helper (14 lines)
- `OpenGram/SuggestionUI/Panels/RephraseCardPanelController.swift` - scrollWrapper property, verticalSafeMargin constant, testHookPanel accessor, revised sizing block, updated hide()

## Decisions Made

- `capHeight` positioned immediately before `clampedY` in enum body — logical grouping (size transform helpers together)
- `capped.height < flooredSize.height` condition (strict less-than) — equality means no overflow, no scroll wrapper needed
- Up-front `newPanel.contentView = hosting` removed; contentView now set conditionally after sizing (required by both branches needing to be the setter)

## Deviations from Plan

None - plan executed exactly as written.

## Issues Encountered

None.

## Self-Check

- [x] `PanelPositioner.swift` exists and contains `static func capHeight`
- [x] `RephraseCardPanelController.swift` contains scrollWrapper, NSScrollView, capHeight call, verticalSafeMargin, testHookPanel
- [x] Commit `0ba5712` exists (Task 1)
- [x] Commit `9cbf97a` exists (Task 2)
- [x] `xcodebuild` BUILD SUCCEEDED after both tasks

## Self-Check: PASSED

## Next Phase Readiness

Plan 03 can now add regression tests using `testHookPanel` accessor and the established `makeShortViewModel`/`makeLongViewModel` scaffolding. All production code for D-04/D-05/D-06/D-08/D-09/D-10 is in place.

---
*Phase: 18.3-rephrase-card-panel-sizing-fix-inserted*
*Completed: 2026-04-17*
