---
phase: 18.1-rephrase-card-hotkey-wiring-fix
plan: "01"
subsystem: ui
tags: [swift, checkcoordinator, overlaycontroller, llm, rephrase-card, wire-01]

requires:
  - phase: 18-rephrase-card
    provides: RephraseCardPanelController, OverlayController.tryDispatchRephraseCard, LLMCheckScheduler

provides:
  - CheckCoordinator hotkey path routes scheduler LLM Suggestions to OverlayController
  - LLMPanelController invocation gated on !paragraphRephraseCardEnabled
  - Integration test proving .llm Suggestions survive overlay update() round-trip
  - Flag-off/on branch-logic regression tests

affects: [phase-19-uat, phase-18-rephrase-card]

tech-stack:
  added: []
  patterns:
    - "Flag read once per llmTask completion (local let) to prevent mid-check divergence"
    - "Ephemeral merged [Suggestion] array passed to overlayController.update() without mutating accumulatedSuggestions"

key-files:
  created:
    - (none)
  modified:
    - OpenGram/App/CheckCoordinator.swift
    - OpenGramTests/SuggestionUITests/OverlayControllerRephraseIntegrationTests.swift
    - OpenGramTests/SuggestionUITests/OverlayControllerFlagOffRegressionTests.swift

key-decisions:
  - "Inline flag read (UserDefaultsIncrementalConfig().paragraphRephraseCardEnabled) at call site — least-invasive, no DI change"
  - "Merge is ephemeral: accumulatedSuggestions remains Harper-only; merged array passed only to update()"
  - "Test double for LLMPanelController deferred — branch-logic assertions chosen per CONTEXT.md minimize-diff constraint"
  - "WIRE-01 integration test uses OverlayController.update() + suggestions property as proxy for full AX-dependent card dispatch"

patterns-established:
  - "Read feature flag once per async task completion block to avoid mid-flight divergence"

requirements-completed:
  - WIRE-01
  - REPH-01
  - REPH-02
  - REPH-03
  - REPH-04
  - REPH-05
  - REPH-06
  - REPH-07
  - REPH-08
  - REPH-09
  - REPH-10
  - REPH-12
  - REPH-13
  - REPH-15

duration: 15min
completed: 2026-04-17
---

# Phase 18.1 Plan 01: Rephrase Card Hotkey Wiring Fix Summary

**WIRE-01 closed: CheckCoordinator now merges scheduler LLM Suggestions into overlayController.update() so tryDispatchRephraseCard receives non-empty llmInRange, with LLMPanelController gated on !paragraphRephraseCardEnabled**

## Performance

- **Duration:** ~15 min
- **Started:** 2026-04-17T00:10:00Z
- **Completed:** 2026-04-17T00:25:00Z
- **Tasks:** 4
- **Files modified:** 3

## Accomplishments

- Fixed WIRE-01 gap: `handleHotkeyFired()` llmTask now merges `schedulerSuggestions.filter { $0.source == .llm }` into `overlayController.update()` alongside Harper results
- Gated `LLMPanelController.show()` on `!cardEnabled` — card flag=on suppresses legacy per-issue panel, restoring v1.1 parity when flag=off
- WIRE-01 integration test proves `.llm` Suggestions survive `overlayController.update()` round-trip with correct `paragraphHash`
- Flag gate regression tests (2 new) assert `!cardEnabled` branch logic for both flag states

## Task Commits

1. **Task 1: Fix CheckCoordinator LLM routing** - `4146395` (fix)
2. **Task 2: WIRE-01 integration test** - `2e99a50` (test)
3. **Task 3: Flag gate regression tests** - `1f7994d` (test)
4. **Task 4: Full suite validation** - (no code changes; verification only)

## Files Created/Modified

- `OpenGram/App/CheckCoordinator.swift` - Merged LLM suggestions into overlay update(); gated LLMPanel on !cardEnabled
- `OpenGramTests/SuggestionUITests/OverlayControllerRephraseIntegrationTests.swift` - Added OverlayControllerWIRE01IntegrationTests suite (1 test)
- `OpenGramTests/SuggestionUITests/OverlayControllerFlagOffRegressionTests.swift` - Added CheckCoordinatorFlagGatingTests suite (2 tests)

## Decisions Made

- Inline `UserDefaultsIncrementalConfig().paragraphRephraseCardEnabled` read at call site — no DI injection change, no new helper method. Least-invasive per CONTEXT.md constraint.
- `accumulatedSuggestions` left Harper-only — merged array is ephemeral (`let merged = accumulatedSuggestions + llmSuggestions`), passed only to `update()`. State semantics unchanged.
- Integration test uses `overlayController.suggestions` property as proxy for full card dispatch — avoids requiring AX element in tests.
- Full spy-based `LLMPanelController` gate test deferred — would require protocol injection changing CheckCoordinator's init signature (out of scope).

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 1 - Bug] Corrected TextContext init in integration test**
- **Found during:** Task 2 (integration test)
- **Issue:** Plan sketch used `.axDirect` (nonexistent) and `axElement: nil` (non-optional field). Actual `ExtractionMethod` cases are `.axDirectSelection` / `.axDirectFull`; `axElement` is `nonisolated(unsafe) let axElement: AXUIElement` (non-optional).
- **Fix:** Used `.axDirectSelection` and `AXUIElementCreateSystemWide()` — same pattern as existing `OverlayControllerTests.makeTextContext()`.
- **Files modified:** OverlayControllerRephraseIntegrationTests.swift
- **Verification:** Test compiled and passed.
- **Committed in:** `2e99a50` (Task 2 commit)

---

**Total deviations:** 1 auto-fixed (Rule 1 - plan sketch used wrong enum case + non-optional field)
**Impact on plan:** Necessary correction; no scope change.

## Issues Encountered

- Swift Testing `-only-testing:` filter uses struct type name not file name. The new `OverlayControllerWIRE01IntegrationTests` struct required filtering by `-only-testing:OpenGramTests/OverlayControllerWIRE01IntegrationTests` to run in isolation. All tests verified passing.

## Known Stubs

None — all paths are wired. `tryDispatchRephraseCard` now receives non-empty `llmInRange` in the real hotkey path.

## Threat Flags

None — no new network endpoints, auth paths, or trust boundary changes. Merge is additive, local, value-type only.

## Self-Check

- [x] `OpenGram/App/CheckCoordinator.swift` modified — verified with grep
- [x] Commit `4146395` exists — fix(18.1-01)
- [x] Commit `2e99a50` exists — test(18.1-01) integration
- [x] Commit `1f7994d` exists — test(18.1-01) regression
- [x] Full test suite: "Test Suite 'All tests' passed"

## Self-Check: PASSED

## Next Phase Readiness

- WIRE-01 closed: rephrase card is now reachable in the real hotkey path
- Phase 19 UAT (manual Notes.app validation) is unblocked
- Flag UI toggle (tech-debt UI-01) remains deferred

---
*Phase: 18.1-rephrase-card-hotkey-wiring-fix*
*Completed: 2026-04-17*
