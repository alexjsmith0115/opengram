---
name: Phase 18.1 — Rephrase Card Hotkey Wiring Fix — Context
description: WIRE-01 gap closure — route scheduler LLM Suggestions into OverlayController so card dispatches in hotkey path
phase: 18.1
milestone: v1.2
gathered: 2026-04-17
status: Ready for planning
mode: Auto-generated (urgent insertion from v1.2 milestone audit)
---

# Phase 18.1: Rephrase Card Hotkey Wiring Fix — Context

<domain>
## Phase Boundary

Fix WIRE-01 from v1.2 milestone audit. Phase 18 shipped the rephrase card components — `RephraseCardView`, `RephraseCardPanelController`, `OverlayController.tryDispatchRephraseCard`, `DisplayHeuristic`, `LLMCheckScheduler.markDismissed`, `AXTextReplacer`. All unit-verified. But `CheckCoordinator.handleHotkeyFired()` routes scheduler LLM `Suggestion` objects exclusively to the legacy `LLMPanelController` (v1.1 per-issue panel path) — `OverlayController` only ever receives Harper suggestions. `OverlayController.tryDispatchRephraseCard()` filters `.source == .llm` but always receives an empty `llmInRange` set → card cannot fire in the real hotkey path under any flag combination.

Out of scope for 18.1:
- UI toggles for feature flags (deferred tech-debt UI-01)
- Phase 19 UAT (manual Notes.app validation)
- AppDelegate DI duplication cleanup (low-severity tech debt)
- Any rework of card visual/interaction behavior — 18-UI-SPEC contract is frozen
</domain>

<decisions>
## Implementation Decisions

### Routing strategy
- After `scheduler.check()` in `CheckCoordinator.handleHotkeyFired()`, merge scheduler LLM Suggestions into `overlayController.update(...)` call alongside Harper suggestions (single `[Suggestion]` payload).
- Keep `LLMPanelController` invocation for backwards-compat behind `!config.paragraphRephraseCardEnabled` gate. When card flag is ON, the legacy panel must NOT fire — card owns the presentation.
- When card flag is OFF, preserve existing v1.1 behavior byte-identical: per-issue LLM popover via `LLMPanelController`.

### Test strategy
- New integration test in `OverlayControllerRephraseIntegrationTests.swift` (or new file) that constructs a real `OverlayController` + mock `LLMCheckScheduler` + stub context, dispatches a qualifying LLM `Suggestion`, and asserts `tryDispatchRephraseCard` fires + `RephraseCardPanelController.show` is called with correct ViewModel.
- Extend `OverlayControllerFlagOffRegressionTests` to assert `LLMPanelController` is invoked when flag=false AND not invoked when flag=true (mock or spy on the legacy panel).
- Existing 441 tests must remain green.

### Requirements bookkeeping
- Update REQUIREMENTS.md traceability rows for INCR-02, INCR-04, INCR-09, INCR-13, SET-08, SET-09 from `[ ]` to `[x]` — these are stale entries surfaced by the milestone audit (REQ-STALE-01). Commit separately from code fix.

### Claude's Discretion
- Whether to introduce a thin helper method on `CheckCoordinator` (e.g., `mergedSuggestionsForOverlay`) vs inline merge at the call site — pick the clearer option.
- Whether to fold flag-gating into `CheckCoordinator` or into `LLMPanelController`'s own show method — pick the least-invasive path that preserves flag-off parity.
- Test-double shape for `LLMPanelController` spy.

</decisions>

<code_context>
## Existing Code Insights

### Gap site
- `OpenGram/App/CheckCoordinator.swift:110` — Harper suggestions currently passed to `overlayController.update()`. LLM Suggestions added alongside here.
- `OpenGram/App/CheckCoordinator.swift:141–161` — scheduler LLM results currently mapped to `[LLMStyleSuggestion]` and routed exclusively to `showLLMPanel()`. Needs gating on `!paragraphRephraseCardEnabled`.

### Card dispatch (already correct, just not receiving input)
- `OpenGram/SuggestionUI/Overlay/OverlayController.swift:355` — `llmInRange` filter for `.source == .llm && paragraphHash == hash` (always empty today).
- `OpenGram/SuggestionUI/Overlay/OverlayController.swift:420–463` — accept/dismiss closures construction.

### Config gate
- `OpenGram/CheckEngine/LLMCheckScheduler/IncrementalConfig.swift` — `paragraphRephraseCardEnabled: Bool`
- `OpenGram/CheckEngine/LLMCheckScheduler/UserDefaultsIncrementalConfig.swift` — reads `llmParagraphRephraseCardEnabled` key live

### Existing tests to extend/mirror
- `OpenGramTests/SuggestionUITests/OverlayControllerRephraseIntegrationTests.swift` (new integration suite)
- `OpenGramTests/SuggestionUITests/OverlayControllerFlagOffRegressionTests.swift` (extend flag-off parity)
- `OpenGramTests/AppDelegateWiringTests.swift` (DI regression — may need update if constructor changes)

### Pre-existing flaky tests (tolerate; pass in isolation)
- `AXCallWatchdogTests.blocklistExpires`
- `LLMCheckSchedulerCancellationTests.idleDebounceSeconds_liveReadHonoredWithoutReinit`
</code_context>

<specifics>
## Specific Ideas

- Minimize diff to `CheckCoordinator` — this is high-traffic integration code.
- Don't reintroduce a separate LLM `[Suggestion]` → `[LLMStyleSuggestion]` → `[Suggestion]` round-trip; the scheduler returns `[Suggestion]` directly and `OverlayController.update()` accepts `[Suggestion]` — pass them through.
- If a shared state property like `OverlayController.incrementalConfig.paragraphRephraseCardEnabled` is read both by card dispatch and by flag-off gating, read once per hotkey to avoid mid-check divergence.
</specifics>

<deferred>
## Deferred Ideas

- UI toggle for `llmParagraphRephraseCardEnabled` + `llmIncrementalCheckingEnabled` in Advanced Settings — tracked as tech-debt UI-01, defer to a future decimal phase or v1.3 if rollout gate is intentional.
- AppDelegate duplicate `UserDefaultsIncrementalConfig` instance consolidation — low-severity, defer.
- 18-08 Task 4 manual Notes.app UAT — remains deferred to Phase 19 UAT, but will ACTUALLY be runnable once this phase lands.

</deferred>
