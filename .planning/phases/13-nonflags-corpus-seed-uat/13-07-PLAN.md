---
phase: 13
plan: 07
type: execute
wave: 5
depends_on: [13-04, 13-05, 13-06]
files_modified:
  - .planning/phases/13-nonflags-corpus-seed-uat/13-VERIFICATION.md
autonomous: false
requirements: [CLAR-21]
must_haves:
  truths:
    - "Final phase gate green: cargo test full suite + xcodebuild app build + xcodebuild test full suite"
    - "13-VERIFICATION.md exists with human_verification YAML for 3 UAT scenarios"
    - "UAT scenarios attempt computer-use MCP first; fall back to user-driven script if /Applications blocker recurs"
    - "All 5 ROADMAP success criteria for Phase 13 verified or human_needed-flagged"
  artifacts:
    - path: ".planning/phases/13-nonflags-corpus-seed-uat/13-VERIFICATION.md"
      provides: "Phase verification record with build gates + 3-scenario UAT script + sign-off"
      contains: "human_verification:"
  key_links:
    - from: "13-VERIFICATION.md::human_verification"
      to: "Notes.app + TextEdit + LLM endpoint"
      via: "3 scenario specs"
      pattern: "Scenario [0-9]"
---

<objective>
Final phase gate. Run all build + test commands and capture results in `13-VERIFICATION.md`. Then drive 3-scenario manual UAT via computer-use MCP (or user-driven fallback per Phase 12 `/Applications` blocker pattern).

3 UAT scenarios per CONTEXT + RESEARCH §Manual UAT Script:
1. Notes.app — wordy phrase end-to-end (utilize + in order to → 2 solid-orange clarity underlines; Accept replaces; Dismiss suppresses)
2. TextEdit — clarity master toggle behavior (master OFF suppresses; master ON re-surfaces). NOTE: corpus has zero `severity = "low"` entries (verified via grep) — sub-toggle ON path cannot surface new entries; document this deviation in scenario 2.
3. LLM `.tone`+`.rephrase` enabled → zero `.clarity` source entries surface (Phase 7 invariant end-to-end)

Per CONTEXT test wiring order: UAT runs LAST (after corpus seed + LLM regression test + docs).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/13-nonflags-corpus-seed-uat/13-CONTEXT.md
@.planning/phases/13-nonflags-corpus-seed-uat/13-RESEARCH.md
@.planning/phases/13-nonflags-corpus-seed-uat/13-VALIDATION.md
@.planning/phases/13-nonflags-corpus-seed-uat/13-04-SUMMARY.md
@.planning/phases/13-nonflags-corpus-seed-uat/13-05-SUMMARY.md
@.planning/phases/13-nonflags-corpus-seed-uat/13-06-SUMMARY.md

@.planning/phases/12-settings-ui-severity-filter-acknowledgements/12-VERIFICATION.md
@.planning/phases/12-settings-ui-severity-filter-acknowledgements/12-03-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run full build + test gates and capture results into 13-VERIFICATION.md scaffold</name>
  <files>.planning/phases/13-nonflags-corpus-seed-uat/13-VERIFICATION.md</files>
  <read_first>
    - .planning/phases/12-settings-ui-severity-filter-acknowledgements/12-VERIFICATION.md (frontmatter + human_verification YAML format precedent — match structure)
    - .planning/phases/13-nonflags-corpus-seed-uat/13-VALIDATION.md (per-task verification map)
  </read_first>
  <action>
1. Run all 3 build/test gates and capture output:

```bash
# A. xcodebuild app build
xcodebuild -project /Users/alex/Dev/opengram/OpenGram.xcodeproj -scheme OpenGram build 2>&1 | tee /tmp/13-build.log | tail -5

# B. xcodebuild test (full Swift suite)
xcodebuild -project /Users/alex/Dev/opengram/OpenGram.xcodeproj -scheme OpenGram test -destination 'platform=macOS' 2>&1 | tee /tmp/13-swift-test.log | tail -10

# C. cargo test (full Rust suite)
~/.rustup/toolchains/stable-aarch64-apple-darwin/bin/cargo test --manifest-path /Users/alex/Dev/opengram/harper-bridge/Cargo.toml 2>&1 | tee /tmp/13-cargo.log | tail -10
```

All three MUST succeed:
- A: `BUILD SUCCEEDED`
- B: `TEST SUCCEEDED`
- C: All `cargo test` lines show `test result: ok`

If any fails: STOP. Fix the failure (do not paper over it) before proceeding to Task 2. The phase gate is a hard stop.

2. Create `/Users/alex/Dev/opengram/.planning/phases/13-nonflags-corpus-seed-uat/13-VERIFICATION.md`. Use Write tool.

Required content (mirror Phase 12 structure verbatim):

YAML frontmatter — replace `passed | failed` placeholders with actual outcome (literal `passed` or literal `failed` based on captured command exit). Do NOT leave the literal string `passed | failed` in the file:
```yaml
---
phase: 13
slug: nonflags-corpus-seed-uat
status: in_progress     # → "passed" or "human_needed" after Task 2
date: 2026-04-25
build_gate:
  xcodebuild_app: <passed-or-failed>
  xcodebuild_test: <passed-or-failed>
  cargo_test: <passed-or-failed>
human_verification:
  - test: "Notes.app — wordy phrase end-to-end (UAT Scenario 1)"
    expected: "Solid orange underlines on 'utilize' + 'in order to'; popover footer 'Clarity'; Accept replaces text; Dismiss removes underline"
    why_human: "Cross-app AX surface — automation cannot drive Notes.app + AX overlay reliably"
    status: pending
  - test: "TextEdit — clarity master toggle behavior (UAT Scenario 2)"
    expected: "Master OFF suppresses all orange clarity underlines; master ON re-surfaces them. NOTE: corpus has zero severity=low entries; opinionated sub-toggle ON has no new visible effect this phase — document and skip sub-toggle assertion"
    why_human: "End-to-end Settings → notification observer → FFI → re-check flow; visual underline state requires user eye"
    status: pending
  - test: "LLM .clarity suppression end-to-end (UAT Scenario 3)"
    expected: "Configured LLM with .tone + .rephrase ON; ZERO popover footers read 'Clarity' for LLM-source suggestions (Phase 7 invariant intact)"
    why_human: "Requires user-configured LLM endpoint + cross-app AX surface; Phase 7 contract end-to-end"
    status: pending
---
```

Body sections (markdown):

1. `# Phase 13 — Verification` H1.
2. `## Build Gate` H2 with the 3 captured outputs (last 5-10 lines from each tee'd log) inside fenced bash blocks. Mark each gate `passed` or `failed`.
3. `## Automated Test Gate` H2:
   - cargo test --test nonflags_corpus output (5 fns passing)
   - xcodebuild test --only-testing:OpenGramTests/LLMServiceTests/parseClarityCategoryDropped_CLAR21 output
4. `## Manual UAT Script` H2 with the 3 scenarios reproduced verbatim from RESEARCH §Manual UAT Script (steps + expected). Reference each scenario by number.
5. `## Sign-Off` H2 with `[ ]` checkboxes for each scenario + final `Phase 13 status:` line (placeholder for Task 2 to update).

Constraints:
- Reference `CLAR-21` as the requirement ID. No `Phase N` ref except in section heads (those are documentation, not source code).
- Do NOT mark scenarios `passed` yet — Task 2 handles that.
- Build gate values fill in based on actual command outputs — substitute literal `passed` or `failed`; the literal token sequence `passed | failed` MUST NOT remain in the final file.
  </action>
  <verify>
    <automated>test -f /Users/alex/Dev/opengram/.planning/phases/13-nonflags-corpus-seed-uat/13-VERIFICATION.md && grep -q "human_verification:" /Users/alex/Dev/opengram/.planning/phases/13-nonflags-corpus-seed-uat/13-VERIFICATION.md && grep -q "Scenario 1" /Users/alex/Dev/opengram/.planning/phases/13-nonflags-corpus-seed-uat/13-VERIFICATION.md && grep -q "Scenario 2" /Users/alex/Dev/opengram/.planning/phases/13-nonflags-corpus-seed-uat/13-VERIFICATION.md && grep -q "Scenario 3" /Users/alex/Dev/opengram/.planning/phases/13-nonflags-corpus-seed-uat/13-VERIFICATION.md && ! grep -q 'passed | failed' /Users/alex/Dev/opengram/.planning/phases/13-nonflags-corpus-seed-uat/13-VERIFICATION.md && xcodebuild -project /Users/alex/Dev/opengram/OpenGram.xcodeproj -scheme OpenGram build 2>&1 | tail -3 | grep -q "BUILD SUCCEEDED" && ~/.rustup/toolchains/stable-aarch64-apple-darwin/bin/cargo test --manifest-path /Users/alex/Dev/opengram/harper-bridge/Cargo.toml 2>&1 | tail -5 | grep -q "test result: ok"</automated>
  </verify>
  <acceptance_criteria>
    - `xcodebuild -project OpenGram.xcodeproj -scheme OpenGram build` exits 0 with `BUILD SUCCEEDED`
    - `xcodebuild -project OpenGram.xcodeproj -scheme OpenGram test -destination 'platform=macOS'` exits 0 with `TEST SUCCEEDED`
    - `cargo test --manifest-path harper-bridge/Cargo.toml` exits 0 with all `test result: ok`
    - `13-VERIFICATION.md` exists with frontmatter `phase: 13`, `human_verification:` block, 3 scenarios
    - File contains `build_gate:` section listing the 3 gate results
    - `! grep -q 'passed | failed' 13-VERIFICATION.md` (template placeholder substituted with literal `passed` or `failed`)
    - File contains body sections for Build Gate, Automated Test Gate, Manual UAT Script, Sign-Off
    - File contains the corpus-has-no-low-severity caveat in Scenario 2's expected text
  </acceptance_criteria>
  <done>All 3 build/test gates green; 13-VERIFICATION.md scaffolded with frontmatter + body, ready for UAT execution.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Drive 3-scenario UAT via computer-use MCP (or user-driven fallback) and update 13-VERIFICATION.md</name>
  <what-built>
- ≥100-line NonFlags corpus across 4 .txt files
- nonflags_corpus.rs with 5 test fns (4 per-category + 1 meta)
- LLMServiceTests.parseClarityCategoryDropped_CLAR21 Swift Testing case
- CONTRIBUTING.md + .github/PULL_REQUEST_TEMPLATE.md
- 13-VERIFICATION.md scaffold with build gates passing
  </what-built>
  <how-to-verify>
**Phase 12 known blocker:** computer-use MCP `installed-apps` resolver only enumerates `/Applications` and cannot reach DerivedData-built `OpenGram.app`. If MCP fails to find the dev build, fall back to user-driven script (status `human_needed`).

**Step 0 — Build + launch dev binary:**
```bash
xcodebuild -project /Users/alex/Dev/opengram/OpenGram.xcodeproj -scheme OpenGram build
APP_PATH=$(find ~/Library/Developer/Xcode/DerivedData/OpenGram-*/Build/Products/Debug -name OpenGram.app -maxdepth 2 | head -1)
open "$APP_PATH"
```
Confirm menu-bar icon present. Open Settings → Clarity tab. Verify master toggle ON, sub-toggle visible (state irrelevant — corpus has no low-severity entries).

**Step 1 — Attempt computer-use MCP path:**
1. `mcp__computer-use__request_access` for Notes, TextEdit, OpenGram bundle id `com.opengram.app`.
2. If `request_access` returns granted for OpenGram → proceed with automation per Scenario 1, 2, 3 below.
3. If MCP cannot reach OpenGram (Phase 12 blocker recurs): pivot to user-driven script (Step 3).

**Step 2 — Scenario execution (computer-use OR user-driven):**

**Scenario 1 — Notes.app wordy phrase end-to-end:**
1. Open Notes.app (or Applications → Notes if computer-use), create new note.
2. Type: `We need to utilize this tool in order to make a decision.`
3. Press Ctrl+Shift+G.
4. Expect: solid orange underline under `utilize` + solid orange underline under `in order to` (2 underlines total). Zero red/purple/teal underlines on this sentence.
5. Click `utilize` underline → popover opens → footer reads "Clarity" with `text.magnifyingglass` icon → click "Accept" → Notes text now reads `We need to use this tool in order to make a decision.`
6. Press Ctrl+Shift+G again → click `in order to` underline → click "Dismiss" → underline disappears, text unchanged.
7. Capture screenshot if computer-use; else user notes pass/fail.

**Scenario 2 — TextEdit master toggle behavior:**
1. Open TextEdit, new document.
2. OpenGram Settings → Clarity tab → toggle master OFF.
3. In TextEdit, type same sentence: `We need to utilize this tool in order to make a decision.`
4. Press Ctrl+Shift+G.
5. Expect (master OFF): ZERO orange clarity underlines. Spelling/grammar (if any) still surface.
6. Settings → Clarity → toggle master ON. Sub-toggle: leave AS-IS (no observable difference — corpus has zero low-severity entries; document this).
7. Switch to TextEdit → Ctrl+Shift+G again → expect orange underlines reappear under `utilize` and `in order to`.
8. Capture screenshot or user pass/fail.

**Scenario 3 — LLM `.clarity` suppression end-to-end:**
**Prerequisite:** user-configured LLM endpoint (local llama.cpp / LM Studio / OpenAI API key). If unavailable, mark scenario `human_needed` deferred — automated DTO + LLMService tests already lock the contract.
1. OpenGram Settings → LLM Provider → configure endpoint + enable `.tone` + `.rephrase` (note: `.clarity` toggle no longer exists per CLAR-09).
2. In TextEdit, type: `At this point in time, I think we should utilize the new system in order to finish the work.`
3. Ctrl+Shift+G → wait for LLM round-trip (≤30s).
4. Expect: orange (Harper) underlines under `utilize`, `in order to`, `at this point in time`. ANY purple/teal (LLM) underlines that appear must NOT have category `.clarity` — verified by clicking each non-orange underline and checking popover footer reads "AI" with badge "Tone" or "Rephrase", never "Clarity".

**Step 3 — User-driven fallback:**
If MCP blocked: paste Scenarios 1, 2, 3 verbatim into chat. Wait for user to execute each and reply with PASS/FAIL + brief notes. Update `13-VERIFICATION.md`:
- For each scenario: set `status: passed` or `status: failed` (with failure detail) or `status: human_needed` (LLM endpoint missing).
- Update top-level `status:` to `passed` (all green) or `human_needed` (any pending) or `failed` (any failure).

**Step 4 — Commit:**
After verification record updated, commit:
```bash
git add .planning/phases/13-nonflags-corpus-seed-uat/13-VERIFICATION.md
git commit -m "docs(13-07): UAT verification — Phase 13 sign-off"
```
  </how-to-verify>
  <resume-signal>Reply "approved" if all scenarios pass; reply "human_needed" if Scenario 3 blocked by missing LLM endpoint (or other partial blocker); describe failures otherwise.</resume-signal>
</task>

</tasks>

<verification>
- xcodebuild app build + xcodebuild test full suite + cargo test full suite all green.
- 13-VERIFICATION.md exists with build_gate + human_verification YAML.
- 3 UAT scenarios each marked `passed`, `failed`, or `human_needed` with explicit reason.
- Top-level `status:` resolves to `passed` or `human_needed` (matches Phase 12 pattern).
</verification>

<success_criteria>
1. All 3 automated build/test gates green.
2. 13-VERIFICATION.md captures build gate results + 3 UAT scenario specs + scenario outcomes.
3. UAT executed via computer-use MCP if accessible; user-driven fallback otherwise.
4. Phase 13 sign-off committed (status: passed or human_needed).
5. Scenario 2 documents the corpus-has-no-low-severity caveat (sub-toggle has no observable effect this phase).
</success_criteria>

<output>
After completion, create `.planning/phases/13-nonflags-corpus-seed-uat/13-07-SUMMARY.md`.
</output>
</content>
