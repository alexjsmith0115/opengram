---
phase: 13
plan: 05
type: execute
wave: 1
depends_on: []
files_modified:
  - OpenGramTests/LLMServiceTests.swift
autonomous: true
requirements: [CLAR-21]
must_haves:
  truths:
    - "LLMService.parseJSONContent end-to-end drops {category:clarity} entries from LLM JSON"
    - "Test runs via xcodebuild and exits 0"
    - "Coverage complements existing DTO-layer test at LLMResponseDTOTests.swift:96"
  artifacts:
    - path: "OpenGramTests/LLMServiceTests.swift"
      provides: "Adds parseClarityCategoryDropped_CLAR21 Swift Testing case at LLMService layer"
      contains: "parseClarityCategoryDropped_CLAR21"
  key_links:
    - from: "OpenGramTests/LLMServiceTests.swift::parseClarityCategoryDropped_CLAR21"
      to: "OpenGram/CheckEngine/LLM/LLMService.swift::parseJSONContent"
      via: "direct method call with mocked fenced JSON"
      pattern: "parseJSONContent"
---

<objective>
Add Swift Testing regression case `parseClarityCategoryDropped_CLAR21` to existing `OpenGramTests/LLMServiceTests.swift`. Verifies `LLMService.parseJSONContent` end-to-end drops any LLM JSON entry with `category=clarity` (markdown-fence stripping + preamble stripping + DTO drop integration).

Per RESEARCH §Existing Code Map (gap analysis): existing `LLMResponseDTOTests.swift:96` `clarityCategoryDroppedPostDeletion_CLAR09` covers the DTO layer directly. New test at LLMService layer adds genuine integration coverage (fence stripping + DTO drop together) without redundancy.

Append-only — augments existing file. No new file → no pbxproj edit needed (per RESEARCH §Project test harness).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/13-nonflags-corpus-seed-uat/13-CONTEXT.md
@.planning/phases/13-nonflags-corpus-seed-uat/13-RESEARCH.md

@OpenGramTests/LLMServiceTests.swift
@OpenGramTests/CheckEngine/LLMResponseDTOTests.swift
@OpenGram/CheckEngine/LLM/LLMService.swift
@OpenGram/CheckEngine/LLM/LLMResponseDTO.swift
</context>

<tasks>

<task type="auto">
  <name>Task 1: Investigate parseJSONContent signature + add parseClarityCategoryDropped_CLAR21 Swift Testing case</name>
  <files>OpenGramTests/LLMServiceTests.swift</files>
  <read_first>
    - OpenGramTests/LLMServiceTests.swift (full — locate existing fenced-JSON parse precedent ~line 46-56; match @Test attribute style + Swift Testing #expect imports already in file)
    - OpenGramTests/CheckEngine/LLMResponseDTOTests.swift:80-110 (existing clarityCategoryDroppedPostDeletion_CLAR09 — confirm what it covers + cite in new test's doc comment)
    - OpenGram/CheckEngine/LLM/LLMService.swift (find parseJSONContent — confirm method signature: async vs sync, return type, parameter labels)
    - OpenGram/CheckEngine/LLM/LLMResponseDTO.swift (confirm SuggestionDTO category enum + that unknown rawValue silently drops)
  </read_first>
  <action>
1. Open `OpenGramTests/LLMServiceTests.swift`. Locate the existing fenced-JSON parse test (RESEARCH cites `LLMServiceTests.swift:46-56` — find a similar `@Test` case that calls `parseJSONContent` with a fenced JSON string and asserts on the returned suggestions).

2. Open `OpenGram/CheckEngine/LLM/LLMService.swift`. Confirm the exact signature of `parseJSONContent`. Common patterns:
   - `func parseJSONContent(_ content: String, paragraph: String) async -> [LLMStyleSuggestion]`
   - `func parseJSONContent(_ content: String) -> [LLMStyleSuggestion]`
   Adjust the test call accordingly.

3. Append a new `@Test` case AFTER the existing fenced-JSON test. Use the EXACT same patterns as the surrounding tests (init pattern for LLMService, async/sync, paragraph param if used). Example structure (adjust `parseJSONContent` signature to match actual code):

```swift
@Test("LLMService drops category=clarity entries end-to-end (markdown-fence + DTO drop integration). Complements DTO-layer coverage at LLMResponseDTOTests.clarityCategoryDroppedPostDeletion_CLAR09. CLAR-21.")
func parseClarityCategoryDropped_CLAR21() async {
    let service = LLMService()
    let json = """
    ```json
    {"suggestions": [
      {"category": "clarity", "revised_text": "X", "explanation": "E1", "confidence": 9},
      {"category": "tone", "revised_text": "Y", "explanation": "E2", "confidence": 8}
    ]}
    ```
    """
    // Adjust signature to match actual parseJSONContent API:
    let suggestions = await service.parseJSONContent(json, paragraph: "Original text.")
    #expect(suggestions.count == 1, "clarity entry must be dropped; only tone remains")
    #expect(suggestions.first?.category == .tone)
}
```

If `parseJSONContent` signature differs (sync, no paragraph param, different return type), adjust:
- If sync: drop `await` and `async`.
- If no paragraph param: drop `paragraph:` argument.
- If returns `Result<[...], Error>`: unwrap accordingly.
- If `LLMStyleSuggestion.Category` enum case is different (e.g., `.tone` vs `.toneAndStyle`), match the actual case name.

Constraints:
- Use Swift Testing (`@Test` + `#expect`) — NOT XCTest. Per CLAUDE.md.
- Reference `CLAR-21` in test name and doc string. NO `Phase 13` reference.
- Do NOT touch URLProtocol / network mocks — `parseJSONContent` is pure parse (RESEARCH Pitfall 5).
- Do NOT add a new test file — append to existing `OpenGramTests/LLMServiceTests.swift` (no pbxproj change).

4. Build and run via xcodebuild:
```bash
xcodebuild -project /Users/alex/Dev/opengram/OpenGram.xcodeproj -scheme OpenGram test -destination 'platform=macOS' -only-testing:OpenGramTests/LLMServiceTests/parseClarityCategoryDropped_CLAR21
```
Verify exit 0.
  </action>
  <verify>
    <automated>grep -c "parseClarityCategoryDropped_CLAR21" /Users/alex/Dev/opengram/OpenGramTests/LLMServiceTests.swift | grep -q "^[1-9]" && xcodebuild -project /Users/alex/Dev/opengram/OpenGram.xcodeproj -scheme OpenGram test -destination 'platform=macOS' -only-testing:OpenGramTests/LLMServiceTests/parseClarityCategoryDropped_CLAR21 2>&1 | tail -20 | grep -q "TEST SUCCEEDED"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "parseClarityCategoryDropped_CLAR21" OpenGramTests/LLMServiceTests.swift` returns ≥1
    - `grep -c "@Test" OpenGramTests/LLMServiceTests.swift` count increased by 1 vs pre-edit baseline
    - `grep -c '"category": "clarity"' OpenGramTests/LLMServiceTests.swift` returns ≥1
    - `grep -L "Phase 13" OpenGramTests/LLMServiceTests.swift` returns the file path (no Phase ref)
    - `xcodebuild -project OpenGram.xcodeproj -scheme OpenGram test -destination 'platform=macOS' -only-testing:OpenGramTests/LLMServiceTests/parseClarityCategoryDropped_CLAR21` exits 0 with `TEST SUCCEEDED`
    - Full `xcodebuild -project OpenGram.xcodeproj -scheme OpenGram build` still BUILD SUCCEEDED (no compile regressions)
  </acceptance_criteria>
  <done>parseClarityCategoryDropped_CLAR21 test exists in LLMServiceTests.swift, passes via xcodebuild test invocation, no compile regressions.</done>
</task>

</tasks>

<verification>
- New `@Test` case present in `OpenGramTests/LLMServiceTests.swift`.
- `xcodebuild ... -only-testing:OpenGramTests/LLMServiceTests/parseClarityCategoryDropped_CLAR21` exits 0.
- Full xcodebuild build still green.
- Test references CLAR-21, not Phase 13.
</verification>

<success_criteria>
1. `parseClarityCategoryDropped_CLAR21` Swift Testing case appended to LLMServiceTests.swift.
2. Test passes via xcodebuild.
3. No pbxproj change (file is augmented, not added).
4. Doc comment cites complementary DTO-layer test for traceability.
</success_criteria>

<output>
After completion, create `.planning/phases/13-nonflags-corpus-seed-uat/13-05-SUMMARY.md`.
</output>
