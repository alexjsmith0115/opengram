---
phase: 13
plan: 04
type: execute
wave: 4
depends_on: [13-03]
files_modified:
  - harper-bridge/tests/nonflags_corpus.rs
autonomous: true
requirements: [CLAR-21]
must_haves:
  truths:
    - "Meta test asserts total non-comment line count across all 4 fixture files ≥100"
    - "Test fails fast if anyone accidentally deletes fixture lines below the threshold"
  artifacts:
    - path: "harper-bridge/tests/nonflags_corpus.rs"
      provides: "Adds nonflags_meta_corpus_size test fn (5th test in binary)"
      contains: "fn nonflags_meta_corpus_size"
  key_links:
    - from: "harper-bridge/tests/nonflags_corpus.rs::nonflags_meta_corpus_size"
      to: "harper-bridge/tests/nonflags/*.txt"
      via: "include_str! + parse_fixture_file count"
      pattern: 'parse_fixture_file.*nonflags/.*\.txt'
---

<objective>
Add `nonflags_meta_corpus_size` guard test. Asserts total non-comment lines across all 4 fixture files ≥100. Fail-fast on accidental fixture deletion.

Per CONTEXT discretion #6 + RESEARCH Open Question #6 (RESOLVED yes): cost is one extra test fn; benefit is fail-fast on regression. Add it.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/13-nonflags-corpus-seed-uat/13-03-SUMMARY.md
@harper-bridge/tests/nonflags_corpus.rs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add nonflags_meta_corpus_size test fn to nonflags_corpus.rs</name>
  <files>harper-bridge/tests/nonflags_corpus.rs</files>
  <read_first>
    - harper-bridge/tests/nonflags_corpus.rs (full — append new test fn at end)
  </read_first>
  <action>
Append to `harper-bridge/tests/nonflags_corpus.rs` after the existing 4 test fns:

```rust
#[test]
fn nonflags_meta_corpus_size() {
    // Guard: total non-comment non-blank lines across all 4 fixture files ≥100.
    // Fail-fast on accidental fixture deletion. CLAR-21.
    let total: usize = [
        include_str!("nonflags/proper_nouns.txt"),
        include_str!("nonflags/quoted_code.txt"),
        include_str!("nonflags/domain_terms.txt"),
        include_str!("nonflags/retext_issues.txt"),
    ]
    .iter()
    .map(|raw| parse_fixture_file(raw).len())
    .sum();

    assert!(
        total >= 100,
        "NonFlags corpus shrunk below threshold: total={} non-comment lines (expected ≥100). \
         Did someone delete fixtures? Add lines back to harper-bridge/tests/nonflags/*.txt.",
        total
    );
}
```

Constraints:
- Reuses existing `parse_fixture_file` helper — no new helpers added.
- No `Phase` reference; only `CLAR-21`.
- After append, full `cargo test --test nonflags_corpus` must show 5 passing tests.
  </action>
  <verify>
    <automated>grep -c "fn nonflags_meta_corpus_size" /Users/alex/Dev/opengram/harper-bridge/tests/nonflags_corpus.rs | grep -q "^1$" && ~/.rustup/toolchains/stable-aarch64-apple-darwin/bin/cargo test --test nonflags_corpus --manifest-path /Users/alex/Dev/opengram/harper-bridge/Cargo.toml 2>&1 | tail -10 | grep -E "test result: ok\. 5 passed"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "fn nonflags_meta_corpus_size" harper-bridge/tests/nonflags_corpus.rs` returns 1
    - `grep -c "total >= 100" harper-bridge/tests/nonflags_corpus.rs` returns 1
    - `cargo test --test nonflags_corpus` exits 0 with `5 passed`
    - File contains no `Phase` reference (`grep -L Phase harper-bridge/tests/nonflags_corpus.rs` returns the file path)
    - Total runtime still <5s
  </acceptance_criteria>
  <done>nonflags_meta_corpus_size test fn appended; cargo test reports 5 passing tests.</done>
</task>

</tasks>

<verification>
- 5 test fns total in nonflags_corpus.rs (4 per-category + 1 meta).
- Meta test asserts ≥100 line threshold and passes against current corpus.
- All tests still complete in <5s.
</verification>

<success_criteria>
1. `nonflags_meta_corpus_size` exists and asserts total ≥100.
2. cargo test reports `5 passed`.
3. Meta test would fail-fast if fixture lines deleted below 100.
</success_criteria>

<output>
After completion, create `.planning/phases/13-nonflags-corpus-seed-uat/13-04-SUMMARY.md`.
</output>
