---
phase: 13
plan: 02
type: execute
wave: 2
depends_on: [13-01]
files_modified:
  - harper-bridge/tests/nonflags/proper_nouns.txt
  - harper-bridge/tests/nonflags/quoted_code.txt
autonomous: true
requirements: [CLAR-21]
must_haves:
  truths:
    - "proper_nouns.txt has ≥20 hand-curated fixture lines, all producing zero clarity lints"
    - "quoted_code.txt has ≥25 hand-curated fixture lines, all producing zero clarity lints"
    - "Combined non-comment line count across these 2 files ≥45 (first milestone)"
  artifacts:
    - path: "harper-bridge/tests/nonflags/proper_nouns.txt"
      provides: "≥20 proper-noun NonFlag sentences"
      min_lines: 20
    - path: "harper-bridge/tests/nonflags/quoted_code.txt"
      provides: "≥25 quoted-code/path NonFlag sentences"
      min_lines: 25
  key_links:
    - from: "harper-bridge/tests/nonflags_corpus.rs::nonflags_proper_nouns"
      to: "harper-bridge/tests/nonflags/proper_nouns.txt"
      via: "include_str! + parse_fixture_file"
      pattern: 'include_str!\("nonflags/proper_nouns.txt"\)'
    - from: "harper-bridge/tests/nonflags_corpus.rs::nonflags_quoted_code"
      to: "harper-bridge/tests/nonflags/quoted_code.txt"
      via: "include_str! + parse_fixture_file"
      pattern: 'include_str!\("nonflags/quoted_code.txt"\)'
---

<objective>
Seed first batch of hand-curated NonFlag fixtures: ≥20 proper-noun + ≥25 quoted-code/path entries. Each fixture line MUST produce zero `WordyPhrasesLinter` lints. First milestone of the ≥100-line corpus per CONTEXT test wiring order (empty harness → 25-line milestone → ≥100).

Purpose: Lock regression containment for two highest-yield false-positive buckets (proper nouns + quoted code) before adding harder-to-curate domain-terms + retext-issues.

Output: Two populated .txt files with combined ≥45 non-comment lines; cargo test green.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/13-nonflags-corpus-seed-uat/13-CONTEXT.md
@.planning/phases/13-nonflags-corpus-seed-uat/13-RESEARCH.md
@.planning/phases/13-nonflags-corpus-seed-uat/13-01-SUMMARY.md

@harper-bridge/data/wordy_phrases.toml
@harper-bridge/tests/nonflags_corpus.rs
@harper-bridge/tests/nonflags/proper_nouns.txt
@harper-bridge/tests/nonflags/quoted_code.txt
</context>

<tasks>

<task type="auto">
  <name>Task 1: Seed proper_nouns.txt with ≥20 hand-curated proper-noun NonFlag sentences</name>
  <files>harper-bridge/tests/nonflags/proper_nouns.txt</files>
  <read_first>
    - harper-bridge/tests/nonflags/proper_nouns.txt (preserve existing header)
    - harper-bridge/data/wordy_phrases.toml (sample known wordy phrases — pick proper nouns whose flagged-substring overlap is real)
    - .planning/phases/13-nonflags-corpus-seed-uat/13-RESEARCH.md §50-Fixture Scraping Strategy (curated examples)
  </read_first>
  <action>
Append ≥20 hand-curated sentences to `harper-bridge/tests/nonflags/proper_nouns.txt`. Preserve existing 3-line `#` header. One sentence per line. No `#` comment markers on fixture lines (only on header).

Each sentence MUST contain a proper noun whose tokenization causes a wordy-phrase substring to appear in the document, but the WordyPhrasesLinter MUST NOT flag (because Harper tokenizes proper nouns as opaque Word tokens or because the substring is mid-token).

**CRITICAL pre-validation strategy (apply BEFORE bulk-appending):**

1. Pre-grep `harper-bridge/data/wordy_phrases.toml` to identify single-word entries that would flag as bare proper-noun roots:
   ```bash
   grep -E '^phrase = "[a-z]+"\s*$' harper-bridge/data/wordy_phrases.toml | awk -F'"' '{print $2}' | sort -u
   ```
   Treat the resulting list as the BAN LIST for first-word-of-proper-noun position.

2. **AVOID these single-word phrases verbatim as the FIRST or SOLE word of a proper noun** (the WordyPhrasesLinter token-level matcher will fire on the bare word regardless of capitalization context):
   `Therefore, Furthermore, Approximately, Numerous, Subsequent, Heretofore, Henceforth, Pursuant, Equivalent, Aforementioned, Optimal (n/a single-word — safe), Notable (n/a — safe), Implementation (n/a — safe)`.
   The flagged single-word roots from corpus that MUST NOT lead a fixture proper noun include: `Therefore, Furthermore, Approximately, Numerous, Subsequent, Heretofore, Henceforth, Pursuant, Aforementioned, Equivalent, Acquire, Demonstrate, Endeavor, Inquire (n/a — not in corpus, safe), Currently, Forthwith (n/a — not in corpus, safe), Assistance, Utilize, Implementation (n/a — safe), Notable (n/a — safe), Optimal (n/a — safe)`.

3. **Batch validation protocol:**
   - Append the FIRST 5 fixture lines only.
   - Run `~/.rustup/toolchains/stable-aarch64-apple-darwin/bin/cargo test --test nonflags_corpus nonflags_proper_nouns --manifest-path /Users/alex/Dev/opengram/harper-bridge/Cargo.toml`.
   - If any line flags → identify which one (test failure output names the phrase) → replace with safer alternative (multi-word company name where neither word is in corpus, or proper noun whose flagged-substring is mid-token like `iPhone` or `MacBook`).
   - Repeat until first 5 pass. Then append batch of 5 more, re-run, fix, repeat until ≥20 fixture lines green.

Strategy for safe selection:
1. Use Harper-friendly proper nouns where the flagged substring is internal/mid-token: "iPhone", "MacBook", "WaterShed Inc.", "DataPipeline LLC".
2. Use multi-word company names whose lead word is NOT in the corpus ban list: "Notable Solutions Inc.", "Implementation Studios LLC", "Optimal Solutions Group", "Better Methods Inc.", "Adequate Holdings".
3. Each sentence: subject + proper noun + simple verb + object. Keep grammar clean (avoid spelling/grammar trips).

Suggested fixture lines (USE these or equivalents — total ≥20 fixture lines required). Each lead-word has been cross-checked against the corpus ban list:

```
She works at Quartermile Capital Partners on quarterly reports.
Implementation Studios LLC released a new product last week.
Notable Solutions Inc. ships software to government clients.
The Granite Software Group reported strong earnings.
Whether iPhone or Android, both platforms support our app.
The MacBook Pro keyboard backlight was bright last night.
Mr. Pemberton approved the proposal at the board meeting.
A team from Riverbend Industries arrived this morning.
She joined Lighthouse Logistics as a senior engineer.
Highland Analytics ranked first in the survey.
A representative from Stanwick Holdings called yesterday.
Bridgeport Network Foundation hosts a charity event annually.
The Westbrook Group acquired three startups last year.
A keynote speaker from Cascade Ventures will present.
Greystone Capital filed paperwork with the regulator.
Maple Industries opened a new office in Berlin.
Larkspur Studios produces independent films.
Foundry Robotics demonstrated their latest model.
Optimal Solutions Group acquired Better Methods Inc.
Birchwood Consulting reported record revenue this quarter.
The Brookline Group merged with Adequate Holdings.
Harvest Foods exports cereals to twelve countries.
```

That's 22 fixture lines + 3 header lines = 25 total file lines. ≥20 fixture-line requirement met.

If any sentence above triggers a lint when running cargo test, REPLACE it with a different proper-noun construction whose lead word is NOT in the corpus ban list. Re-run after each batch of 5 lines added (NOT after all 22 at once).
  </action>
  <verify>
    <automated>awk 'NF && !/^#/' harper-bridge/tests/nonflags/proper_nouns.txt | wc -l | awk '$1 >= 20 { exit 0 } { exit 1 }' && ~/.rustup/toolchains/stable-aarch64-apple-darwin/bin/cargo test --test nonflags_corpus nonflags_proper_nouns --manifest-path /Users/alex/Dev/opengram/harper-bridge/Cargo.toml 2>&1 | tail -10 | grep -q "test result: ok"</automated>
  </verify>
  <acceptance_criteria>
    - `awk 'NF && !/^#/' harper-bridge/tests/nonflags/proper_nouns.txt | wc -l` returns ≥20
    - File preserves header lines starting with `#`
    - `~/.rustup/toolchains/stable-aarch64-apple-darwin/bin/cargo test --test nonflags_corpus nonflags_proper_nouns --manifest-path /Users/alex/Dev/opengram/harper-bridge/Cargo.toml` exits 0
    - No fixture line contains the literal string `Phase`
    - Test output shows "test nonflags_proper_nouns ... ok"
  </acceptance_criteria>
  <done>≥20 proper-noun NonFlag fixtures committed; cargo test passes for nonflags_proper_nouns.</done>
</task>

<task type="auto">
  <name>Task 2: Seed quoted_code.txt with ≥25 hand-curated quoted-code/path NonFlag sentences</name>
  <files>harper-bridge/tests/nonflags/quoted_code.txt</files>
  <read_first>
    - harper-bridge/tests/nonflags/quoted_code.txt (preserve header)
    - harper-bridge/data/wordy_phrases.toml (pick phrases that commonly appear in code/path contexts)
    - harper-bridge/tests/nonflags/proper_nouns.txt (sanity-check no overlap)
  </read_first>
  <action>
Append ≥25 hand-curated quoted-code/path sentences to `harper-bridge/tests/nonflags/quoted_code.txt`. Preserve header.

Strategy: each sentence wraps a flagged-phrase substring inside backticks, double quotes, or a path-like token. Harper tokenizes punctuation that breaks word boundaries — matched substrings inside backtick/quote pairs SHOULD not flag because the flagged phrase tokens are interrupted by punctuation.

**Pre-validation batch protocol (same as Task 1):**
- Append first 5 lines, run `cargo test --test nonflags_corpus nonflags_quoted_code`, cull any that flag, replace with safer construction (try double-quotes or underscore-joined snake_case if backticks fail; underscore-joined identifiers like `due_to_the_fact_that` are usually safest because they tokenize as single Word tokens with unrecognized form).
- Then batches of 5 until ≥25 fixture lines green.

**AVOID bare-prose patterns** — every flagged phrase MUST be wrapped in backticks, double-quotes, OR appear inside a snake_case/kebab-case identifier. Do NOT write `Use prior to in your prose` (bare). DO write `Use \`prior to\` in your prose` (wrapped).

Suggested fixture lines (USE or replace; ≥25 fixture lines required):

```
The `utilize` keyword appears in legacy MATLAB documentation.
Use `prior to` only inside a code block, never in prose.
The Bash idiom `due to the fact that` appears in legacy scripts.
Run `make a decision` from the CLI to invoke the helper.
The path `/var/log/in_order_to.log` rotates daily.
The function `at_this_point_in_time()` returns the current epoch.
The variable `subsequent_to_value` holds the next entry.
Execute `terminate the connection` via the admin REST endpoint.
The Python class `Utilize` extends `BaseUtility`.
The Ruby gem `commence-now` is deprecated.
Set `enable.opportunity` to true in the YAML config.
The C macro `IN_THE_EVENT_OF` expands to a goto statement.
Call `request.aforementioned()` to access the prior request.
The TypeScript type `Numerous<T>` allows multiple entries.
The shell alias `cease_and_desist` kills runaway jobs.
The XPath query `//utilize[@version="1"]` selects the node.
Pass `--prior-to-flag` to enable backwards-compat mode.
The header `X-In-Order-To: yes` controls retry behavior.
The SQL column `due_to_the_fact_that` was renamed in v3.
The regex `at\s+this\s+point` matches the literal phrase.
Set the environment variable `UTILIZE_LEGACY=1` to opt in.
The CSS class `.commence-modal` styles the entry dialog.
Reference `RFC 793 in order to` understand the connection lifecycle.
The HTTP header `X-Currently-Status: live` is non-standard.
The npm package `@scope/aforementioned` was unpublished.
The cookie `subsequent_session=true` persists across reloads.
```

26 fixture lines.

After writing, run cargo test in batches per protocol. If any sentence triggers a lint, replace it. Common cause: backticks survive Harper tokenization in some configurations — try double-quotes or snake_case if backticks fail.
  </action>
  <verify>
    <automated>awk 'NF && !/^#/' harper-bridge/tests/nonflags/quoted_code.txt | wc -l | awk '$1 >= 25 { exit 0 } { exit 1 }' && ~/.rustup/toolchains/stable-aarch64-apple-darwin/bin/cargo test --test nonflags_corpus nonflags_quoted_code --manifest-path /Users/alex/Dev/opengram/harper-bridge/Cargo.toml 2>&1 | tail -10 | grep -q "test result: ok"</automated>
  </verify>
  <acceptance_criteria>
    - `awk 'NF && !/^#/' harper-bridge/tests/nonflags/quoted_code.txt | wc -l` returns ≥25
    - File preserves header lines starting with `#`
    - `cargo test --test nonflags_corpus nonflags_quoted_code` exits 0
    - No fixture line contains the literal string `Phase`
    - Combined `awk 'NF && !/^#/' harper-bridge/tests/nonflags/{proper_nouns,quoted_code}.txt | wc -l` returns ≥45
    - Full `cargo test --test nonflags_corpus` exits 0 (all 4 fns still green)
  </acceptance_criteria>
  <done>≥25 quoted-code NonFlag fixtures committed; combined batch 1 ≥45 lines; cargo test passes for both fns.</done>
</task>

</tasks>

<verification>
- `awk 'NF && !/^#/'` count for `proper_nouns.txt` ≥20.
- `awk 'NF && !/^#/'` count for `quoted_code.txt` ≥25.
- `cargo test --test nonflags_corpus` exits 0 with all 4 test fns passing.
- No `Phase` string in any fixture line.
</verification>

<success_criteria>
1. proper_nouns.txt: ≥20 fixture lines, all zero-lint.
2. quoted_code.txt: ≥25 fixture lines, all zero-lint.
3. Combined ≥45 lines (milestone toward ≥100 final).
4. cargo test green for both per-category fns.
</success_criteria>

<output>
After completion, create `.planning/phases/13-nonflags-corpus-seed-uat/13-02-SUMMARY.md`.
</output>
</content>
</invoke>